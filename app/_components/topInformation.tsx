"use server";
import React from "react";

type TopInformationProps = {
    children?: React.ReactNode;
}

export default async function TopInformation({ children }: TopInformationProps): Promise<React.ReactElement> {
    return (
        <div>

        </div>
    )
}