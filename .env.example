# GitHub API Configuration
# Generate a personal access token at: https://github.com/settings/tokens
# For public repositories, you only need 'repo' scope
# For private repositories, you'll need additional permissions

# Option 1: Server-side only (recommended for production)
GITHUB_TOKEN=your_github_personal_access_token_here

# Option 2: Client-side (less secure, for development only)
# NEXT_PUBLIC_GITHUB_TOKEN=your_github_personal_access_token_here

# JWT Configuration  
JWT_SECRET=your_jwt_secret_key_here

# MongoDB Configuration
MONGODB_URI=mongodb://sample.mongodb.com:27017
CLIENT_DB=portfolio

# Cron Job Secret
CRON_SECRET=your_cron_job_secret_key_here